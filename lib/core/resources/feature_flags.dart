class FeatureFlags {
  /// Whether to enable email/password authentication (Sign In & Sign Up).
  /// Set to false for the publish version to remove these features.
  static const bool enablePasswordAuth = true;
}
